# Sequelize Traversy

```sql
CREATE DATABASE sequelize_traversy

USE sequelize_traversy

CREATE TABLE user(
	id INT NOT NULL PRIMARY KEY AUTO_INCREMENT,
	name VARCHAR(250) NOT NULL,
	lastname VARCHAR(250) NOT NULL,
	email VARCHAR(250) NOT NULL
)
```